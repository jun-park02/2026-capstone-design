1. FastAPI가 시작될 때 `FireDetectListener`가 생성되고 백그라운드 태스크로 실행된다.

2. 이 태스크는 `_watch_loop()` 안에서 Redis의 `fire_detect` 스트림을 `xread`로 계속 기다린다. 시작 `last_id`가 `$`라서 서버가 켜진 뒤 들어온 새 이벤트만 받는다.

3. `raw-udp-rx`는 한 이미지의 청크가 전부 모이면 `try_finalize_session()`에서 이미지 바이트를 합친 뒤, 로컬 파일로 저장하지 않고 곧바로 S3에 업로드한다.

4. 업로드 결과로 만들어진 `s3_bucket`, `s3_object_key`, `s3_image_url` 같은 메타데이터를 `publish_completed_event()`가 Redis의 `fire_detect` 스트림에 완료 이벤트로 올린다. 업로드에 실패해도 이벤트는 발행되고, 대신 `s3_upload_status`와 `s3_upload_error`가 함께 실린다.

5. 백엔드 리스너는 그 메시지를 받으면 payload를 JSON으로 풀고, `last_event_id`와 `last_event`를 메모리에 저장한다. 여기에는 Redis 메시지 id, 원본 data, S3 URL이 들어간다.

6. 클라이언트는 `/raw/latest/live`로 메모리에 캐시된 마지막 감지 결과를 바로 볼 수 있고, `/raw/latest`로 Redis의 최신 이벤트를 직접 조회할 수도 있다.

7. 이미지는 더 이상 `/fire-images/...` 같은 로컬 정적 파일 경로로 제공하지 않고, payload 안의 `s3_image_url`만 사용한다.

### 요약

UDP 청크 수신 -> 이미지 조립 -> S3 업로드 -> Redis `fire_detect`에 이벤트 발행 -> `FireDetectListener`가 수신 -> `last_event` 갱신 -> `/raw/latest/live`와 `/raw/latest`에서 S3 URL 포함 응답 제공

### 백그라운드 태스크

백그라운드 태스크는 `start() -> _watch_loop() -> Redis 스트림 대기` 흐름으로 작동한다.

---

### app.state

FastAPI 앱 객체에 붙어 있는 공용 저장공간

- 앱이 실행되는 동안 계속 유지되는 값
- 여러 라우트나 의존성 함수에서 함께 써야하는 객체

---

@asynccontextmanager는 원래 `async with`용 아닌지?

`@asynccontextmanager`는 "이 함수는 `yield`를 기준으로 들어갈 때/나올 때 동작하는 비동기 컨텍스트 매니저다"라고 만들어주는 데코레이터다.
