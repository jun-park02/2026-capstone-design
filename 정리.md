1. FastAPI가 시작될 때 FireDetectListener가 생성되고 백그라운드 태스크로 실행

2. 이 태스크는 _watch_loop() 안에서 Redis의 fire_detect 스트림을 xread로 계속 기다림. 시작 last_id가 `$`라서 서버가 켜진 뒤 들어온 새 이벤트만 받음

3. raw-udp-rx는 한 이미지의 청크가 전부 모이면 try_finalize_session()이 이미지 바이트를 합쳐 파일로 저장하고, publish_completed_event()가 Redis의 fire_detect 스트림에 완료 이벤트를 올림

4. 백엔드 리스너가 그 메시지를 받으면 payload를 JSON으로 풀고, 이미지 파일이 있으면 _maybe_upload_fire_image_to_s3()가 S3 업로드를 시도. 성공하면 s3_image_url도 붙임

5. 그다음 last_event_id와 last_event를 메모리에 저장. 여기에는 Redis 메시지 id, 원본 data, 로컬 이미지 URL, S3 URL이 들어감

6. 클라이언트는 /raw/latest/live로 메모리에 캐시된 마지막 감지 결과를 바로 볼 수 있고, /raw/latest로 Redis의 최신 이벤트를 직접 조회할 수도 있음. 

7. 이미지 자체는 /fire-images/<파일명>으로 접근됨. FastAPI가 /app/images를 정적 파일로 마운트해 놨기 때문.

### 요약

UDP 청크 수신 -> 이미지 조립/저장 -> Redis fire_detect에 이벤트 발행 -> FireDetectListener가 수신 -> (선택) S3 업로드 -> last_event 갱신 -> /raw/latest/live 와 /fire-images/... 로 제공

### 백그라운드 태스크

백그라운드 태스크는 `start() -> _watch_loop() -> Redis 스트림 대기` 이런식으로 작동


---

### app.state

FastAPI 앱 객체에 붙어 있는 공용 저장공간

- 앱이 실행되는 동안 계속 유지되는 값
- 여러 라우트나 의존성 함수에서 함께 써야하는 객체


---

@asynccontextmanager는 원래 async with용 아닌지?

@asynccontextmanager는 "이 함수는 yield를 기준으로 들어갈 때/나올 때 동작하는 비동기 컨텍스트 매니저다"라고 만들어주는 데코레이터.

